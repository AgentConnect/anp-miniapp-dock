# Location Map Preview Fixture

Mock-only MiniApp MCP fixture for Render IR snapshot tests. It exercises static map preview output with opaque location handles and fail-closed Host location boundaries.

